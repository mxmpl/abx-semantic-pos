[project]
name = "abx_semantic_pos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "Maxime Poli" }, { name = "Robin Algayres" }]
dependencies = [
  "fastabx>=0.7.1",
  "polars>=1.39.3",
]

[dependency-groups]
dev = [
  "ipykernel>=7.2.0",
  "prek>=0.3.8",
]

[build-system]
requires = ["flit_core>=3.2,<4"]
build-backend = "flit_core.buildapi"
